#include "dynamic_memory.h"
#include <memory>
#include<iostream>

using std::unique_ptr; using std::make_unique; using std::cout;

int main() 
{
	use_weak_ptr();

	//use_shared_ptr();

	//use_unique_ptr();
	
	return 0;
}